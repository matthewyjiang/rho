# Implementation Plans

Generated by the improve skill on 2026-06-21. Execute in the order below unless dependencies say otherwise. Each executor should read the plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 002 | Add a static cross-provider model catalog | P1 | M | — | DONE |
| 001 | Use a static model list for Codex auth | P1 | S | — | REJECTED: superseded by 002 |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 has no dependencies.
- 001 is superseded by 002 because the desired behavior is a central cross-provider catalog and provider+model switching, not an OpenAI-only Codex fallback.

## Findings considered and rejected

- Fallback inside `src/tui.rs::open_model_picker`: rejected for this issue because the provider already owns model listing. Returning a usable Codex list from `OpenAiProvider::list_models` keeps TUI code simple and fixes any future caller of the provider method.
- Dynamic Codex model listing via `https://chatgpt.com/backend-api/codex`: rejected because the current observed failure says this auth path does not support model listing. A static curated list matches Pi's documented behavior for built-in providers.
- OpenAI-only static fallback in plan 001: rejected because `/model` should query one static catalog across all available providers and switch both provider and model.
